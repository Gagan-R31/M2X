name: Run App Engine Script

on:
  push:
    paths:
      - 'app_engine/**'

jobs:
  run-script:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: |
          cd app_engine
          pip install -r requirements.txt

      - name: Run Python script
        run: |
          cd app_engine
          python main.py
